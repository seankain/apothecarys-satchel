
[package]
name = "game_dylib"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
ApothecaryEditor = { path = "../game", default-features = false }

[features]
default = ["ApothecaryEditor/default"]
dylib-engine = ["ApothecaryEditor/dylib-engine"]
