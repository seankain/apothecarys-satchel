
[package]
name = "executor"
version = "0.1.0"
edition = "2021"

[dependencies]
fyrox = { workspace = true }
ApothecaryEditor = { path = "../game", optional = true }

[features]
default = ["ApothecaryEditor"]
dylib = ["fyrox/dylib"]
