
[package]
name = "executor"
version = "0.1.0"
edition = "2021"

[dependencies]
fyrox = { workspace = true }
apothecarys-satchel = { path = "../game", optional = true }

[features]
default = ["apothecarys-satchel"]
dylib = ["fyrox/dylib"]
